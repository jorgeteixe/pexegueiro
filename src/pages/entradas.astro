---
import { getCollection } from 'astro:content'
import { Icon } from 'astro-icon/components'
import Layout from '../layouts/Layout.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

const entradasEntries = await getCollection('entradas')

// Sort entries by filename (which includes the order)
const sortedEntradas = entradasEntries.sort((a, b) => a.id.localeCompare(b.id))
---

<Layout title="Entradas">
  <div class="flex min-h-screen flex-col">
    <Header />
    
    <main class="flex-grow bg-gray-50">
      <div class="mx-auto max-w-4xl px-4 py-8 sm:px-6">
        
        <!-- Header Section -->
        <div class="mb-8">
          <h1 class="text-3xl font-bold text-gray-900 sm:text-4xl">Entradas</h1>
          <p class="mt-2 text-gray-600">Contido adicional e complementario ao libro</p>
        </div>

        <!-- Entries List -->
        <div class="space-y-6">
          {
            sortedEntradas.map((entrada) => (
              <article class="bg-white rounded-lg border border-gray-200 p-6 hover:border-gray-300 transition-colors">
                <div class="flex items-start justify-between">
                  <div class="flex-1">
                    <h2 class="text-xl font-semibold text-gray-900 mb-2">
                      <a 
                        href={`/entradas/${entrada.slug}`}
                        class="hover:text-blue-600 transition-colors"
                      >
                        {entrada.data.title}
                      </a>
                    </h2>
                    
                    <div class="flex items-center text-sm text-gray-500 mb-4">
                      <Icon name="mdi:update" class="h-4 w-4 mr-1" />
                      <span>Actualizado o {entrada.data.updatedDate.toLocaleDateString('gl-ES')}</span>
                    </div>
                  </div>
                  
                  <div class="flex-shrink-0 ml-4">
                    <div class="rounded-lg bg-gray-50 p-3">
                      <Icon name="mdi:book-open" class="h-6 w-6 text-gray-600" />
                    </div>
                  </div>
                </div>
                
                <div class="mt-4 border-t border-gray-100 pt-4">
                  <a 
                    href={`/entradas/${entrada.slug}`}
                    class="inline-flex items-center text-sm font-medium text-blue-600 hover:text-blue-800 transition-colors"
                  >
                    Ler entrada
                    <Icon name="mdi:arrow-right" class="h-4 w-4 ml-1" />
                  </a>
                </div>
              </article>
            ))
          }
        </div>

        <!-- Info Section -->
        <div class="mt-12 bg-white rounded-lg border border-gray-200 p-8">
          <div class="text-center">
            <Icon name="mdi:book-open" class="mx-auto h-12 w-12 text-gray-400 mb-4" />
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Contido complementario</h2>
            <p class="text-gray-600 max-w-2xl mx-auto">
              Estas entradas conteñen información adicional que complementa o contido principal do libro, 
              proporcionando contexto histórico e cultural sobre o patrimonio muiñeiro de Cesantes.
            </p>
          </div>
        </div>
        
      </div>
    </main>
    
    <Footer />
  </div>
</Layout>