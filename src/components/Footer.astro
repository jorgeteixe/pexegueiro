---
const footerLinks = [
  { name: 'Sobre o proxecto', href: '/sobre-o-proxecto' },
  { name: 'Aviso legal', href: '/aviso-legal' },
  { name: 'Política de privacidade', href: '/politica-privacidade' },
]
---

<footer class="w-full border-t border-gray-200 bg-gray-50">
  <div class="mx-auto max-w-6xl px-4 py-8 sm:px-6">
    <div class="flex flex-col items-center space-y-6">
      <!-- Links -->
      <div class="flex flex-wrap justify-center gap-x-6 gap-y-2 text-sm">
        {
          footerLinks.map((link) => (
            <a
              href={link.href}
              class="text-gray-600 transition-colors hover:text-gray-900 hover:underline"
            >
              {link.name}
            </a>
          ))
        }
      </div>

      <!-- Association Name -->
      <div class="text-center">
        <p class="text-sm text-gray-500">
          Obradoiro de Estudos Locais Fernando Monroy
        </p>
      </div>
    </div>
  </div>
</footer>
